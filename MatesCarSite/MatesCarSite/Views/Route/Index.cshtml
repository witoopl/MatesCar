@model List<Route>
@using MatesCarSite.Resources
@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" class="btn btn-warning">Nowa trasa</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @MatesCarSite.Resources.ColumnNamesForRoutes.Driver
            </th>
            <th>
                @MatesCarSite.Resources.ColumnNamesForRoutes.StartLocation
            </th>
            <th>
                @MatesCarSite.Resources.ColumnNamesForRoutes.EndLocation
            </th>
            <th>
                @MatesCarSite.Resources.ColumnNamesForRoutes.FuelUsage
            </th>
            <th>
                @MatesCarSite.Resources.ColumnNamesForRoutes.ChargeForPassenger
            </th>
            <th>
                @MatesCarSite.Resources.ColumnNamesForRoutes.IsPaid
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @item.Driver
            </td>
            <td>
                @item.StartLocation
            </td>
            <td>
                @item.EndLocation
            </td>
            <td>
                @item.FuelUsage
            </td>
            <td>
                @item.ChargeForPassenger
            </td>
            <td>
                @item.IsFullyPaid
            </td>
            <td>
                <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-primary">@MatesCarSite.Resources.Buttons.Details</a>
                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning">@MatesCarSite.Resources.Buttons.Edit</a>
                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">@MatesCarSite.Resources.Buttons.Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
